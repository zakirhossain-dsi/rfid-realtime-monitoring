package com.tng.rfid.repository;

import com.tng.rfid.entity.DeviceConfig;
import org.springframework.data.repository.CrudRepository;

public interface DeviceConfigRepository extends CrudRepository<DeviceConfig, Integer> {
}
